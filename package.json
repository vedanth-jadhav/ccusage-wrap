{
  "scripts": {
    "dev": "native dev",
    "build": "native build --yes",
    "export": "native export --yes",
    "test": "tsx --test tests/*.test.ts"
  },
  "devDependencies": {
    "@native-sdk/cli": "latest",
    "@native-sdk/core": "latest",
    "tsx": "latest"
  }
}
